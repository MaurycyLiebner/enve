

void render_dab_mask (uint16_t * mask,
                        double x, double y,
                        double radius,
                        double hardness,
                        double aspect_ratio, double angle
                        );
